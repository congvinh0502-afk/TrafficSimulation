/* Đọc hướng dẫn sử dụng ở UserManual */


1. (*) sử dụng `lane` để tạo `road`, một `road` bao gồm một hoặc nhiều `lane`, five-way, four-way, three-way gồm `road` và `intersection` [Minh]
2. (*) tạo hitbox(hộp chạm/ biên giới) để kiểm tra xem các thực thể có chạm nhau hay không, chạy song song với code giữ khoảng cách [Minh]
3. (*) thêm "gia tốc" (acceleration) thay vì sử dụng tốc độ và hướng, tốc độ = tốc độ + gia tốc mỗi khung hình(frame) [Minh]
4. (*) thêm lựa chọn quay trở lại menu khi đang chạy chương trình [Minh]
5. (*) thêm hệ thống di chuyển góc quay phóng to, thu nhỏ màn hình [Minh]
6. (*) khoảng cách `direction` của `lane` hiện tại đang sử dụng "north", "south",..... Đề xuất thay đổi bằng vector [Minh]
6. (*) các phương tiện hiện đang teleport ở ngã rẽ, chưa tìm thấy nguyên nhân [Minh]
7. (\) 5-way: bố cục các làn không cân xứng, cây mọc trên đường [Hương]
8. (-) xe di chuyển trên 3-way như 4-way, 3-way lỗi xe chạy trên cỏ {Hương}
9. (*) xe di chuyển không thẳng [Hương]
10.(-) cho xe chay cham lai [toan]
<<<<<<< HEAD
11.(/) bo sung them am thanh cho cac loai xe [toan]
12.(-) 3-way và 5-way đang bị phụ thuộc vào logic phát triển của 4-way -> lỗi, cần fix lại logic intersect {Hương}
13.(-) 5-way đang là 4 nhánh vuông + 1 nhánh chéo -> 5 nhánh đều {Hương}
   (+) Direction (enum cứng)  →  DirectionVector (góc + vector, dynamic) {Hương}
   Khi đó:
    3-way: 3 nhánh cách nhau 120° (hoặc T-shape tùy chọn)
    4-way: 4 nhánh 90°
    5-way: 5 nhánh 72° thật sự
    6-way, 8-way, mạng lưới: thêm vào dễ dàng
    Road network: mỗi intersection có list nhánh riêng, xe navigate bằng graph
14.(\) Logic xe tệ: chỉ chạy trên 4 hướng mặc định Đông - Tây - Nam - Bắc dẫn đến ngoại trừ ngã tư thì tất cả các nhánh còn lại đều lỗi
