/* Đọc hướng dẫn sử dụng ở UserManual */


1. (-) sử dụng `lane` để tạo `road`, một `road` bao gồm một hoặc nhiều `lane`, five-way, four-way, three-way gồm `road` và `intersection` [Minh]
2. (*) tạo hitbox(hộp chạm/ biên giới) để kiểm tra xem các thực thể có chạm nhau hay không, chạy song song với code giữ khoảng cách [Minh]
3. (-) thêm "gia tốc" (acceleration) thay vì sử dụng tốc độ và hướng, tốc độ = tốc độ + gia tốc mỗi khung hình(frame) [Minh]
4. (-) thêm lựa chọn quay trở lại menu khi đang chạy chương trình [Minh]
5. (-) thêm hệ thống di chuyển góc quay phóng to, thu nhỏ màn hình [Minh]
6. (*) khoảng cách `direction` của `lane` hiện tại đang sử dụng "north", "south",..... Đề xuất thay đổi bằng vector [Minh]
6. (-) các phương tiện hiện đang teleport ở ngã rẽ, chưa tìm thấy nguyên nhân [Minh]
7. (\) 5-way: bố cục các làn không cân xứng, cây mọc trên đường [Hương]
8. (-) xe di chuyển trên 3-way như 4-way {Hương}
9. (-) 3 - way lỗi xe chạy trên cỏ [Hương]
10.(-) cho xe chay cham lai [toan]
11.(/) bo sung them am thanh cho cac loai xe [toan]
12.(-) logic giao lộ tệ: 3-way và 5-way đang bị phụ thuộc vào logic phát triển của 4-way -> lỗi, cần fix lại logic intersect {Hương}
13.(-) 5-way đang là 4 nhánh vuông + 1 nhánh chéo -> 5 nhánh đều {Hương}
   (+) Direction (enum cứng)  →  DirectionVector (góc + vector, dynamic) {Hương}
   Khi đó:
    3-way: 3 nhánh cách nhau 120° (hoặc T-shape tùy chọn)
    4-way: 4 nhánh 90°
    5-way: 5 nhánh 72° thật sự
    6-way, 8-way, mạng lưới: thêm vào dễ dàng
    Road network: mỗi intersection có list nhánh riêng, xe navigate bằng graph
14.(-) logic xe tệ: chỉ chạy trên 4 hướng mặc định Đông - Tây - Nam - Bắc dẫn đến ngoại trừ ngã tư thì tất cả các nhánh còn lại đều lỗi[Hương]
    (+)[Hương]
    - sửa Direction.java: Xóa bỏ khái niệm N, S, E, W.Thay vào đó là các vector tương ứng các góc 0°, 72°, 144°, 216°, 288°.
    - sửa LaneAlignmentSystem: Không thể dùng vehicle.setX(...) hay vehicle.setY(...) để căn làn nữa, mà phải dùng toán học lượng giác(Math.sin, Math.cos) để chiếu tọa độ xe lên trục của từng làn đường nghiêng.
    - sửa TurningSystem: Phải tính toán nội suy vòng cung(bezier curve) hoặc xoay xe theo góc 72 độ khi rẽ, thay vì quay 90 độ như hiện tại.
15. (\) map ở 5-way còn lỗi hiển thị nhiều [Hương]
16. (-) cảnh vật xung quanh: cây cối, đèn giao thông, bãi đậu xe... được đặt mặc định -> rất khó trong việc mở rộng thêm loại giao lộ mới, hoặc thay đổi bố cục đường xá. [Hương]
    (+) cần chuyển từ phương pháp Hardcode(gắn tọa độ chết) sang phương pháp Procedural Generation(Sinh tự động theo thuật toán lưới). Thuật toán này hoạt động dựa trên nguyên lý: Quét toàn bộ bản đồ và chỉ đặt vật thể ở những nơi không có mặt đường.
17. (-) lỗi logic xe chạy ở 3-way & 4-way -> khi cập nhật chỉnh sửa 5-way mới lộ rõ: [Hương]
    - ngã 4: Code cũ cấu hình sai hướng đi thẳng. Xe đang đi thẳng lên hướng Bắc lại bị code ép quay ngoắt 180 độ sang hướng Nam, khiến xe tự nhiên xoay vòng vòng giữa ngã tư.
    - ngã 3(hình chữ T, không có đường bên dưới): Nhánh đường từ trên cắm xuống bị cấu hình sai vector(đáng lẽ phải đi xuống thì code cũ lại cho đi ngược lên trời nên xe bay ra ngoài). Thêm nữa, xe không bị cấm rẽ vào nhánh dưới nên cứ thế lao thẳng xuống bãi cỏ.
18. (-) chỉnh bố cục 3-way, 4-way cân xứng với map [Hương]
19. (*) xe còn đâm vào nhau, chèn lên nhau, nhất là ở chỗ mấy khúc cua [Hương]
20. (*) traffic light ở bên trái đường, ngã 4 có 2 cái traffic light hoạt động kiểu gì thế???? [Minh]
   (\) đang thêm [toàn]

